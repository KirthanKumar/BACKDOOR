# BACKDOOR
 socket.py/.exe which is a backdoor that runs on the victim machine helps in executing command by receiving command from my machine, and sending back the output also. A socket_listener.py/.exe which we use as an interface to send command that is executed on victim machine. Downloading, uploading, persisting session, using social engineering etc.
